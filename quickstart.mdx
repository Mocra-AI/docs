---
title: "Quickstart"
description: "Start increasing the observability of your video generation workflow with Mocra."
---

## Getting started

### Step 1: Choose and set up your environment

<AccordionGroup>
  <Accordion icon="terminal" title="HTTP API">
    Mocra has a powerful RESTful HTTP API accessible at `https://api.mocra.io/`.
    You can use it to interact with Mocra if you prefer an HTTP interface or if
    your favorite programming language is not listed below.

    Using the HTTP API does not require installing anything as long as your
    language or environment can perform HTTP operations.

  </Accordion>
  <Accordion icon="node" title="NodeJS SDK">
    If you prefer an SDK, we currently provide a driver for Node.js. To use the
    NodeJS SDK, you will need to install it. Run the following command:
    ```bash
    npm install @mocra/observe
    ```

    This installation includes TypeScript typings in case you want to use them.

  </Accordion>
  <Accordion icon="python" title="Python SDK">
    We currently do not have a native Python SDK, but it will be coming soon.
    Stay updated!
    <Tip>
      Subscribe to our newsletter by emailing `contact@mocra.io` to receive
      updates regarding development.
    </Tip>
  </Accordion>
</AccordionGroup>

### Step 2: Get a Mocra API key

An API key is required to interact with Mocra.

Currently, Mocra is in beta trial, so a limited number of API keys are being issued to selected customers.
If you are interested in using Mocra, email `contact@mocra.io` to join our waitlist.

### Step 3: Write your code

<AccordionGroup>
  <Accordion icon="terminal" title="HTTP API">
    You can use the HTTP API from any environment that supports HTTP requests. Here is an example using cURL:
    ```bash
    curl \
      -X POST \
      -H "Authorization: Bearer <YOUR API KEY>" \
      -H "Content-Type: application/json" \
      -H "Accept: application/json" \
      -d "{\
          \"videoUrl\": \"<YOUR VIDEO URL>\",\
          \"customCriteria\": [],\
          \"removeCriteria\": []\
      }"\
      https://api.mocra.io/observe
    ```
  </Accordion>
  <Accordion icon="node" title="NodeJS SDK">
    Import the required functions from `@mocra/observe` and use them as in the example that follows:
    ```typescript
    import { VideoObservabilityApi } from "@mocra/observe";

    new VideoObservabilityApi("<YOUR API KEY>") // Replace with your actual API key
      .scoreVideo(
        "http://example.org/video.mp4", // Replace with your actual video to analyze
        [
          {
            criterionName: "Blur",
            criterionDescription:
              "The video should not be blurry",
          },
        ],
        ["UNNATURAL PHYSICS"],
      )
      .then((result) => console.log(result));
    ```

  </Accordion>
  <Accordion icon="python" title="Python SDK">
    We currently do not have a native Python SDK, but it will be coming soon.
    Stay updated!
    <Tip>
      Subscribe to our newsletter by emailing `contact@mocra.io` to receive
      updates regarding development.
    </Tip>
  </Accordion>
</AccordionGroup>

<Tip>
  Feel free to use your favorite LLM to write code that interacts with Mocra.
</Tip>

## Next steps

Now that you're set up, explore these key features:

<CardGroup cols={2}>

<Card
  title="Contribute to Mocra"
  icon="square-code"
  href="https://github.com/Mocra-AI/Mocra"
>
  Contribute to Mocra for a more stable and feature-rich SDK.
</Card>

<Card title="API documentation" icon="code" href="/api-reference/introduction">
  Read the full documentation for a more detailed explanation of how to use
  Mocra's observability functionality.
</Card>

</CardGroup>
